apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
  namespace: cnrprod1725741033-team-78604
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app-container
        image: kwantova/my-app:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_ADDRESS
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: SERVER_ADDRESS
        - name: POSTGRES_CONN
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_CONN
        - name: POSTGRES_PASSWORD
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_PASSWORD
